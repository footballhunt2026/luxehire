# Copy this file to .env and fill in.

# Port the server listens on
PORT=3000

# Admin password — used ONLY the first time server ever runs (creates account).
# Set this before first run. If left blank, server generates random password
# and prints it once in the console log.
ADMIN_PASSWORD=ChangeThisNow123!
